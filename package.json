{
    "name": "server",
    "version": "1.0.0",
    "scripts": {
        "client": "cd management && yarn start",
        "server": "nodemon server.js",
        "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\" "
    },
    "dependencies": {
        "body-parser": "^1.20.1",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "concurrently": "^5.0.0"
    }
}
